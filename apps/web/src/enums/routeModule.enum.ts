export enum ApplicationModules {
  user = 'user',
  role = 'role',
  settings = 'settings',
  report = 'report',
  tax = 'tax',
}

export enum ModuleOperations {
  create = 'create',
  update = 'update',
  delete = 'delete',
  view = 'view',
}
