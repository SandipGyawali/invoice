interface Props {
  currentStep: number;
  setCurrentStep: () => void;
}

function SignUpStep1Form({ currentStep, setCurrentStep }: Props) {
  return <>Signup step 1 form</>;
}

export default SignUpStep1Form;
